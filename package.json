{
  "scripts": {
    "test": "nyc --report-dir=qa/coverage --reporter=html-spa --reporter=clover mocha 'test/*.test.js' && sabik --outputReportPath=qa/metrics src",
    "metrics": "sabik --outputReportPath=qa/metrics src",
    "packages": "npm list --all",
    "outdated": "npm outdated --depth=0"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "nyc": "^15.1.0",
    "sabik": "^0.2.4"
  }
}
